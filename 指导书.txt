修正能力项目以及愿景：要形成一个API之间对话、实现、整合、得出结论的AI平台，在当前要求的基础上达成这个流程：步骤零：在模型配置中添加API以及对应baseURL，为平台添加模型；。步骤一：根据分配好的任务做环境检查以及动态调整。比如做一个AI爬取网站信息的任务，Agent调用网页浏览器工具去目标网站“踩点“，发现有严厉的反爬比如Cloudflare验证码，那么就记录网站有反爬，放弃常规request方案；调整为使用Selenium\Playwright模拟浏览器方案。步骤二：骨架生成与分段编写：针对用户输入的目标进行解构，按照总体目标、任务清单、核心约束分解，然后API之间讨论、分配任务。，不要让Agent一次性写出千行代码，应该让它采用”自顶向下“的设计，先写主题框架，再逐个填充细节。步骤三：代码静态检查与自测；脚本写完之后不能直接交给用户，而是Agent需要在后台纠错。实现方式：沙盒环境，agent将脚本丢进沙盒中执行，成功进入下一步，失败则记录报错，将报错作为上下文丢回agent：”刚刚写的代码报错AttributeError，根据报错重新修改脚本。步骤四：业务审查，代码没有报错只说明运行正常，不是结果正确。例如：脚本运行成功，但是抓取下来的数据是空的。审查实现方式：引入Criti作为审查Prompt，核对：目标：。。。。当前结果：。。。。。来动态调整；如果审查通过触发交付，审查不通过re-plan，重新规划，Agent修改定位符，重编代码。按照上述的流程查看、更新目前的D:\CursorProject\docs\tasks


不完整，平台的目标仍然是多Agent沟通平台，刚刚的内容只是对具体实施操作的补充，最开始的愿景仍是：为多个模型创造共享「聊天室」。产品已确认的聊天交互范式（chat review）：用户提问 → ①分析问题并交给某角色回答 → ②不重复角色修订完善 → ③评判角色决定是否通过；未通过则重复②；通过则作为最终回复。每个角色必须独立思考、会话隔离，不得共用同一模型缓存结果；这里补充的是执行阶段的要求。同时，爬取数据只是作为例子，实际要求能利用大模型思考能力处理其他问题，比如生成小游戏、做excel表、读取总结文档等等


在当前要求的基础上达成这个流程：步骤零：在模型配置中添加API以及对应baseURL，为平台添加模型；。步骤一：根据分配好的任务做环境检查以及动态调整。比如做一个AI爬取网站信息的任务，Agent调用网页浏览器工具去目标网站“踩点“，发现有严厉的反爬比如Cloudflare验证码，那么就记录网站有反爬，放弃常规request方案；调整为使用Selenium\Playwright模拟浏览器方案。步骤二：骨架生成与分段编写：针对用户输入的目标进行解构，按照总体目标、任务清单、核心约束分解，然后API之间讨论、分配任务。，不要让Agent一次性写出千行代码，应该让它采用”自顶向下“的设计，先写主题框架，再逐个填充细节。步骤三：代码静态检查与自测；脚本写完之后不能直接交给用户，而是Agent需要在后台纠错。实现方式：沙盒环境，agent将脚本丢进沙盒中执行，成功进入下一步，失败则记录报错，将报错作为上下文丢回agent：”刚刚写的代码报错AttributeError，根据报错重新修改脚本。步骤四：业务审查，代码没有报错只说明运行正常，不是结果正确。例如：脚本运行成功，但是抓取下来的数据是空的。审查实现方式：引入Criti作为审查Prompt，核对：目标：。。。。当前结果：。。。。。来动态调整；如果审查通过触发交付，审查不通过re-plan，重新规划，Agent修改定位符，重编代码。按照上述的流程查看、更新目前的

描述一个Agent智能体所需的功能以及实现，不涉及代码，只给出清单。要求：智能体能从多个开源/闭源模型API作为决策，模型间不互相共享缓存，相互独立，模型间能够自闭环实现意见交流，由一个指定模型作为最终决策者，随后调用工具在电脑/服务器中修改，像cursor一样完成文档生成、内容概括等任务。

补充：运行在Windows；形态是多Agent「聊天室」；能力域需覆盖：多Agent编排、持久身份、跨模型互审、A2A通信、共享记忆、skills、MCP、协作纪律。请输出「多Agent聊天室能力项.md」与「多模型协作Agent功能清单.md」。生成文件位置：D:\CursorProject