# 多 Agent 聊天室能力项

> **产品愿景**：为多个开源/闭源模型创造共享「聊天室」，实现多 Agent 沟通平台。  
> **主交互范式（Chat Review v2）**：共享可见稿 + 自主认领职责 + 已读且无补丁才算同意 + 仅实质局部补丁 + 评判点合入/打回/微调；首答模型 ≠ 评判模型；单模型时用户当评判。  
> **硬约束**：每个参与者独立思考、会话隔离，不得共用同一模型缓存结果。  
> **实施补充**：需落盘/跑脚本时再叠加执行阶段（步骤零→四）；爬取仅为示例之一。  
> 运行于 Windows；本文档为能力项清单（不含实现代码）。

---

## 0. 愿景与层次关系（先读）

| 层次 | 是什么 | 说明 |
|------|--------|------|
| 主目标 | 多模型共享聊天室 | 平台首先是沟通与评审场所，不是单一任务流水线产品 |
| 主范式 | Chat Review v2 | 首答基准稿 → 全员可见审阅（沉默或局部补丁）→ 评判合入/打回 → 确认轮 → 最终回复 |
| 隔离原则 | 独立会话 | 各 Agent 模型会话独立，禁止共享供应商侧缓存 |
| 实施补充 | 执行五段（§13） | 仅当任务需要工具落地时启用 |
| 能力域广度 | 多样化思考任务 | 小游戏、Excel、读文档总结、方案、爬取等均可 |

---

## 1. 聊天室形态与交互壳层

| 编号 | 能力项 | 说明 |
|------|--------|------|
| CR-01 | 共享聊天室 | 多模型同房；以共享工作稿 + 补丁流 + 裁定消息呈现协作 |
| CR-02 | 用户席位 | 提问、打断、必要时当评判（单模型降级）；只消费最终回复或自行裁定 |
| CR-03 | Agent 席位 | 稳定身份；本轮职责可自主认领（首答/审阅/评判），受资格锁约束 |
| CR-04 | 房间即沟通容器 | 先 chat review；执行/交付为可触发子阶段 |
| CR-05 | 共享稿全可见 | 基准稿、有效补丁、评判裁定对参与 Agent 默认可见 |
| CR-06 | 沉默即少言 | 无实质分歧则不发言；禁止附和与琐碎润色刷屏 |
| CR-07 | 议程板 | 展示：首答→审阅窗→合入/打回→+1 确认轮→最终回复；执行子阶段按需展开 |
| CR-08 | 区块化工作稿 | 首答自动/显式切块（段落、列表、代码块等带区块 ID） |
| CR-09 | 引用与补丁 | 发言可引用区块 ID、用户原话、工具输出 |
| CR-10 | 打断与升用户 | 可中断循环；连续打回或预算耗尽可升级用户 |
| CR-11 | 资格锁展示 | UI/状态标明本轮谁首答、谁无评判资格 |
| CR-12 | Windows 原生体验 | 窗口、托盘、快捷键、拖放、路径、通知 |
| CR-13 | 最终回复槽位 | 仅确认轮通过后的稿进入最终回复；过程可折叠 |
| CR-14 | 原话钉选 | 用户原始输入常驻可引用，供评判防漂移 |

---

## 1.1 主范式：Chat Review v2

> 房间默认按本节运转。职责可自主切换，但本轮资格锁定；同意仅「已读 + 无补丁」。

### 1.1.1 主循环与角色

| 编号 | 能力项 | 说明 |
|------|--------|------|
| CH-01 | 用户提问 | 进入本轮；原始输入作为不可漂移的目标锚点 |
| CH-02 | 自主认领职责 | Agent 自行选择本轮职责（首答/审阅/评判）；开轮竞选窗后职责冻结 |
| CH-03 | 首答基准稿 | 认领首答者产出带区块 ID 的共享稿（可并行首答后选定一版为基准） |
| CH-04 | 全员可见审阅 | 各 Agent 独立阅读共享稿；须先「已读」 |
| CH-05 | 沉默同意 | **仅「已读 + 本轮无有效补丁」算同意**；超时未表态 ≠ 同意 |
| CH-06 | 实质局部补丁 | 有分歧则只对分歧区块发 PATCH，禁止无授权整篇重写 |
| CH-07 | 琐碎禁言 | 非关键方向修改不得发言（见 CH-20）；系统直接拒收 |
| CH-08 | 评判点合入 | **任何**有效实质补丁（含单条无冲突）均须评判点击合入后才进稿 |
| CH-09 | 冲突直接合并 | 同区块多实质补丁时，不开多轮辩论，由评判一次合并该块并附说明 |
| CH-10 | 合并后 +1 确认轮 | 合入/微调/局部打回重交后再开一轮；该轮后冲突仍评判合并，确认轮封顶 |
| CH-11 | 通过→最终回复 | 确认轮内有效审阅者均已读且无补丁，且评判宣布通过 → 最终回复 |
| CH-12 | 会话隔离 | 各参与者独立模型会话；不得共用模型缓存 |
| CH-13 | 首答模型 ≠ 评判模型 | 硬约束；本轮产生过首答的模型不得任评判 |
| CH-14 | 单模型降级 A | 房间仅一模型时：**用户当评判**；禁止该模型自动宣布通过 |
| CH-15 | 评判不参与过程讨论 | 评判无闲聊频道；上下文=用户原话+当前共享稿+待决补丁+规则 |
| CH-16 | 与执行衔接 | 需落地时挂载 §13；产物仍回 chat review 门禁 |

### 1.1.2 局部补丁规则

| 编号 | 能力项 | 说明 |
|------|--------|------|
| CH-17 | 一贴一块 | 一条 PATCH 只改一个区块 ID；多块则多条 |
| CH-18 | 补丁必填 | target、substantive 类型、claim（错在哪）、replace（块替换或 diff） |
| CH-19 | 未点名区块锁定 | 未针对的区块视为共识，不得顺手改 |
| CH-20 | 琐碎定义（禁发） | 仅形容词/语法/标点/同义润色；加不重要步骤；去重步骤且不改语义；净变更&lt;100 字且未声明实质类型 |
| CH-21 | 实质定义（可发） | 函数/接口/控制流；实现方案/选型；验收与约束；事实结论；致命遗漏/错误（即便&lt;100 字） |
| CH-22 | 禁止全量重生 | 除非方向打回后的新首答或显式 REWRITE 授权 |
| CH-23 | 依赖声明 | 改 A 必影响 B 时声明 implies，可附带第二条 PATCH |
| CH-24 | 系统拒收无效贴 | 琐碎/无 claim/无 target/超范围 → 不计发言、不打断收敛 |

### 1.1.3 评判裁定与打回分档

| 编号 | 能力项 | 说明 |
|------|--------|------|
| CH-25 | 裁定消息类型 | 仅 VERDICT / MERGE / TWEAK / REJECT，不参与讨论 |
| CH-26 | R1 局部打回 | 方向大体对；只打回若干区块并说明问题；其余区块锁定接受 |
| CH-27 | R2 方向打回（策略 A） | 方案方向错误；**全文基准稿作废**，仅 `keep` 列表可作参考；须新首答；必填锚定用户原话的方向说明书 |
| CH-28 | R3 评判微调 | 不经他模评论，直接对函数/实现小疵微调合入；不得改方案方向/API 契约/验收；须留 diff |
| CH-29 | R2 与「大部分接受」互斥 | 一旦 R2，不得同时宣布大面积终稿接受 |
| CH-30 | R1+R3 可并存 | 可大部接受并微调，同时少数块打回 |
| CH-31 | 有打回则无最终回复 | 存在未关闭的 reject 块时不得出最终回复 |
| CH-32 | R2 不合格拒收 | 「再优化」「文笔差」等非方向理由不得方向打回 |
| CH-33 | 同块连续打回上限 | 超限升级用户或换作答模型 |
| CH-34 | 微调后仍进 +1 轮 | 制衡评判微调权；他模可对变动块提实质异议 |

**Chat Review v2 节拍示意：**

```
用户提问（原话锚定）
  → 自主认领：首答 / 审阅 / 评判（首答模型≠评判模型；单模型→用户评判）
  → 首答产出区块化共享稿
  → 审阅窗：已读；无实质问题则沉默；有则只发实质 PATCH（琐碎拒收）
  → 评判：点合入（含单补丁）/ 冲突则直接合并 / R1·R2·R3
        R2(A)：旧稿作废，keep 仅参考 → 新首答
  → +1 确认轮（已读+无补丁 → 最终回复；有实质补丁 → 再评判点合入，确认轮封顶）
  → 若需落地 → 执行补充 §13 → 再回门禁
```

---

## 2. 多 Agent 编排

| 编号 | 能力项 | 说明 |
|------|--------|------|
| OR-01 | Agent 注册与上线 | 动态注册（模型源、能力标签、权限、工具白名单） |
| OR-02 | 本轮职责认领 | 开轮竞选首答/审阅/评判；窗后冻结；非终身固定职业 |
| OR-03 | 工作流编排 | **主流程 Chat Review v2**；需落地再挂执行子流程 |
| OR-04 | 资格锁 | 本轮已首答的模型不可任评判；补丁作者不可单独宣布通过 |
| OR-05 | 条件分支 | 按补丁/冲突/R1·R2·R3/是否需执行/用户指令分支 |
| OR-06 | 并行首答可选 | 多人抢首答时可并行出稿再选定基准 |
| OR-07 | 超时与降级 | 超时换人；单模型走用户评判降级 A |
| OR-08 | 人工闸门 | 高风险落盘、R2 次数过多、critical 冲突可强制用户 |
| OR-09 | 会话恢复 | 从共享稿版本、未决补丁、确认轮状态恢复 |
| OR-10 | 成本预算 | 限制补丁数、打回次数、确认轮；超限升用户 |
| OR-11 | 执行子流程门控 | 按需启用步骤零→四 |
| OR-12 | 执行失败回跳 | 回补丁/re-plan，再经评判门 |
| OR-13 | 模型隔离硬规则 | 首答模型 ≠ 评判模型；唯一例外为降级 A（用户评判） |

---

## 3. 持久身份

| 编号 | 能力项 | 说明 |
|------|--------|------|
| ID-01 | Agent 稳定身份 | 每个 Agent 拥有跨会话不变的 ID、名称、角色画像 |
| ID-02 | 人格与职责档案 | 持久保存风格偏好、专业域、禁区、输出规范 |
| ID-03 | 信誉与历史表现 | 记录该 Agent 在历史协作中的准确度、冲突率、被采纳率 |
| ID-04 | 权限画像 | 持久绑定可读路径、可写路径、可调用 MCP/工具范围 |
| ID-05 | 模型绑定策略 | 身份可绑定主模型与备用模型，但不共享运行时缓存 |
| ID-06 | 用户身份上下文 | 用户偏好、项目习惯、批准习惯可被房间长期引用 |
| ID-07 | 身份隔离 | 不同 Agent 身份数据隔离；禁止冒充其他 Agent 发言 |
| ID-08 | 身份迁移 | 更换底层模型 API 后，身份档案可迁移，历史信誉可保留或重置 |

---

## 4. 跨模型互审

| 编号 | 能力项 | 说明 |
|------|--------|------|
| RV-01 | 提案互审 | Agent A 产出方案后，由 B/C 独立审查漏洞、遗漏、风险 |
| RV-02 | 盲审模式 | 互审时隐藏原作者身份标签，减少权威偏见 |
| RV-03 | 对抗式批评 | 指定「红队」Agent 专门找反例与失败路径 |
| RV-04 | 一致性核对 | 多模型对同一事实/约束分别作答，标记分歧点 |
| RV-05 | 证据要求 | 互审意见需引用消息、记忆、工具结果或外部依据 |
| RV-06 | 评分与投票 | 对候选方案打分、排序或加权投票（最终仍可由决策者裁定） |
| RV-07 | 修订闭环 | 被驳回方案自动进入修订轮，直至收敛或用户裁断 |
| RV-08 | 互审报告 | 产出结构化分歧表：同意点、争议点、未验证点、建议动作 |
| RV-09 | 与 Chat Review v2 对齐 | 主互审=共享稿审阅+实质补丁+评判合入/R1·R2·R3；执行期结果审查为按需补充 |

---

## 5. A2A 通信（Agent-to-Agent）

| 编号 | 能力项 | 说明 |
|------|--------|------|
| A2A-01 | 点对点私信 | 两 Agent 之间定向交流，可选择是否同步到房间主频道 |
| A2A-02 | 广播与组播 | 向全体或指定 Agent 组广播议题与请求 |
| A2A-03 | 消息类型协议 | 已读、PATCH、MERGE 申请、VERDICT、R1/R2/R3、弃权、工具请求等 |
| A2A-04 | 职责竞选协议 | 开轮认领首答/审阅/评判；冲突时改派或并行首答 |
| A2A-05 | 审阅自闭环 | 用户静默时按审阅窗→评判→确认轮运转直至最终回复或达上限 |
| A2A-06 | 沟通前后工具钩子 | 发言前可检索/计算；发言后可落盘、调用 MCP、触发校验 |
| A2A-07 | 用户-模型通道 | 用户指令可注入任一 Agent，也可广播给全体 |
| A2A-08 | 模型-模型通道 | 不同开源/闭源 API 后端之间通过平台中继对话，不直接共享上下文缓存 |
| A2A-09 | 会话信封 | 每条 A2A 消息携带：发送者、接收者、任务 ID、轮次、可见性、附件引用 |
| A2A-10 | 死信与重试 | 对方超时、解析失败时的重试、改派与死信队列 |

---

## 6. 共享记忆与独立运行时

| 编号 | 能力项 | 说明 |
|------|--------|------|
| MEM-01 | 房间共享记忆 | 任务目标、已确认事实、决议、待办对全体或授权成员可见 |
| MEM-02 | 私有工作记忆 | 每个 Agent 仅自己可见的草稿、中间推理、临时笔记 |
| MEM-03 | 模型缓存隔离 | 各模型 API 会话/KV/前缀缓存互不共享；平台层显式传递所需上下文 |
| MEM-04 | 记忆分层 | 短期对话记忆、任务记忆、项目记忆、长期知识库分层存储 |
| MEM-05 | 记忆写入纪律 | 何种结论可写入共享记忆（需决策者确认或互审通过） |
| MEM-06 | 记忆检索 | 按语义、标签、时间、任务、Agent 来源检索 |
| MEM-07 | 记忆冲突处理 | 新结论与旧记忆冲突时标记版本，由决策者裁定覆盖或并存 |
| MEM-08 | 遗忘与归档 | 过期任务记忆归档；敏感内容可擦除并留审计痕 |
| MEM-09 | 上下文装配 | 为某 Agent 发言组装「仅其权限可见」的最小充分上下文包 |
| MEM-10 | 交付物索引 | 文档、摘要、代码变更、报告等交付产物纳入可检索记忆 |

---

## 7. Skills（技能包）

| 编号 | 能力项 | 说明 |
|------|--------|------|
| SK-01 | Skill 注册 | 将可复用能力（写作、概括、评审、调研、改文件流程）注册为 Skill |
| SK-02 | Skill 绑定 | 指定哪些 Agent 可加载哪些 Skill |
| SK-03 | Skill 触发 | 由用户指令、决策者指派、或房间阶段自动触发 |
| SK-04 | 文档生成 Skill | 大纲→起草→修订→评判→定稿落盘 |
| SK-05 | 内容概括 Skill | 读文档/多文件汇总、要点、对比表、执行摘要 |
| SK-06 | 实现/修改 Skill | 小游戏、脚本、配置等：骨架→分段填充→落盘 |
| SK-07 | 表格/Excel Skill | 生成或填写表格、结构化数据表 |
| SK-08 | 研究/环境侦察 Skill | 按任务需要做检索或浏览器踩点（爬取等场景为例，非唯一） |
| SK-09 | 沙盒自测 Skill | 可执行产物入沙盒；失败回灌实现角色修订 |
| SK-10 | 业务结果审查 Skill | 对照「目标 vs 当前结果」（可与 chat review 评判衔接） |
| SK-11 | Skill 组合 | Chat Review v2 为主链；执行类 Skill 按需挂载 |
| SK-12 | Skill 版本与回滚 | Skill 可版本化；失败可回退 |

---

## 8. MCP（Model Context Protocol）集成

| 编号 | 能力项 | 说明 |
|------|--------|------|
| MCP-01 | MCP 服务器接入 | 连接文件系统、浏览器、数据库、工单、云服务等 MCP Server |
| MCP-02 | 工具发现 | 运行时枚举可用 MCP 工具及其输入 schema |
| MCP-03 | 权限门控 | 按 Agent 身份与房间策略限制可调用的 MCP 工具 |
| MCP-04 | 调用前后钩子 | 调用前校验参数与风险；调用后写入共享记忆/审计日志 |
| MCP-05 | 多 Agent 共用 MCP | 多 Agent 可共享同一 MCP 连接池，但调用会话与凭证按身份隔离 |
| MCP-06 | 结果回灌聊天室 | MCP 工具输出以结构化消息进入房间，供互审与决策 |
| MCP-07 | Windows 本地 MCP | 优先支持本机文件、终端、系统信息、剪贴板等 Windows 场景 |
| MCP-08 | 远程/服务器 MCP | 支持指向远程主机或服务器侧工具，完成远端修改与部署类任务 |
| MCP-09 | 失败语义统一 | MCP 超时、鉴权失败、参数错误映射为房间可读错误事件 |
| MCP-10 | 工具白名单策略包 | 可按任务类型套用「只读」「可写项目内」「可写系统」等策略 |

---

## 9. 协作纪律

| 编号 | 能力项 | 说明 |
|------|--------|------|
| DIS-01 | 发言配额 | 限制冗长输出；禁止附和；琐碎补丁系统拒收 |
| DIS-02 | 证据优先 | 实质补丁须有 claim；无依据降权 |
| DIS-03 | 沉默同意定义 | 仅已读+无有效补丁算同意；超时≠同意 |
| DIS-04 | 确认轮通过才最终回复 | 有未决打回/未点合入补丁时不得最终回复 |
| DIS-05 | 决策可追溯 | 记录首答、补丁、合入、R1/R2/R3、确认轮状态 |
| DIS-06 | 对话与执行分层 | 纯沟通走 Chat Review v2；落地再开执行子流程 |
| DIS-07 | 防串台 | 消息绑定身份；禁止伪造他者结论 |
| DIS-08 | 防无限循环 | 补丁轮、+1 确认轮、R2 次数、沙盒修订均设上限 |
| DIS-09 | 安全纪律 | 高危二次确认；密钥脱敏；沙盒隔离 |
| DIS-10 | 交付验收纪律 | 文本：确认轮通过；产物：另按需沙盒/结果审查 |
| DIS-11 | 补丁礼仪 | 一贴一块、有 claim、禁整篇重生 |
| DIS-12 | 审计与回放 | 房间事件可回放 |
| DIS-13 | 禁止千行倾倒 | 编码类超限须拆段 |
| DIS-14 | 环境检查按需 | 无外部依赖可不侦察 |
| DIS-15 | 首答/评判模型隔离 | 同模型不得既首答又评判；单模型→用户评判 |
| DIS-16 | 评判过程隔离 | 评判不参与讨论；直读用户原话防漂移 |
| DIS-17 | R2 策略 A | 方向打回则旧稿作废，keep 仅参考 |
| DIS-18 | 冲突少轮次 | 补丁冲突由评判直接合并，默认不开互辩多轮 |

---

## 10. 决策大脑与模型接入

| 编号 | 能力项 | 说明 |
|------|--------|------|
| BR-01 | 多模型 API 接入 | 同时接入多个开源/闭源模型 API 作为各 Agent 决策大脑 |
| BR-02 | 一 Agent 一运行时 | 每个 Agent 的模型调用上下文独立，不与其他 Agent 共享供应商侧缓存 |
| BR-03 | 评判者指定 | 多模型时由非首答模型认领评判；单模型时强制用户评判 |
| BR-04 | 评判合并与打回 | 点合入、冲突合并、R1/R2(A)/R3；上下文直读用户原话 |
| BR-05 | 备用大脑切换 | 主模型不可用时切换备用 API，身份与房间状态保持 |
| BR-06 | 能力路由 | 按任务类型把子问题路由到更擅长的模型 Agent |
| BR-07 | 输出规范化 | 统一各模型输出为房间协议消息（结论、问题、工具请求、反对意见） |
| BR-08 | 模型配置录入（步骤零） | 在模型配置中填写 API Key、对应 baseURL、模型名等，将模型注册进平台目录后才可分配给 Agent |
| BR-09 | 连通性自检 | 新增模型后对 baseURL 做探活/试调用；失败不得进入任务房间作为大脑 |
| BR-10 | 多端点共存 | 允许同一厂商不同 baseURL、或兼容 OpenAI 协议的自建/代理端点并存 |

---

## 11. 工具执行与环境作用（类 Cursor）

| 编号 | 能力项 | 说明 |
|------|--------|------|
| EX-01 | 沟通前工具 | 发言前检索文件、搜索记忆、读取 MCP 资源以形成依据 |
| EX-02 | 沟通后工具 | 发言/决策后创建文档、改代码、写摘要、更新任务板 |
| EX-03 | 本机修改 | 在 Windows 本机项目/目录中创建、编辑、删除（受权限约束） |
| EX-04 | 服务器修改 | 经授权通道在远端服务器执行变更、部署或配置 |
| EX-05 | 文档生成交付 | 报告、方案、说明书、纪要等 |
| EX-06 | 内容概括交付 | 读文档/多轮讨论的摘要与要点 |
| EX-07 | 多样化交付 | 小游戏、Excel/表格、代码、清单、计划、评审意见等（不限于爬取） |
| EX-08 | 执行回执 | 工具成败、diff、产物路径、沙盒日志、审查结论回写房间 |
| EX-09 | 回滚能力 | 策略允许时的撤销/回滚 |
| EX-10 | 按需环境侦察 | 任务需要时用浏览器等工具踩点；爬取反爬仅为示例场景 |
| EX-11 | 方案动态切换 | 依据侦察结论切换技术路线（示例：request → Playwright） |
| EX-12 | 自顶向下分段编写 | 编码类任务：先骨架再分段；禁止一次倾倒超长代码 |
| EX-13 | 沙盒后台自测 | 可执行产物先沙盒纠错，再进入结果审查 |
| EX-14 | 结果正确性审查 | 运行无报错仍须对照目标检查产物（空表、空数据、不可玩等均不通过） |
| EX-15 | 审查失败 re-plan | 不通过则重规划/重改，再经修订或执行闭环 |

---

## 12. 房间生命周期与运维

| 编号 | 能力项 | 说明 |
|------|--------|------|
| LF-01 | 创建/归档/重开房间 | 任务级房间全生命周期管理 |
| LF-02 | 快照 | 保存某一时刻的成员、记忆、议程、未决分歧 |
| LF-03 | 导出 | 导出对话、决议、交付物清单为可读包 |
| LF-04 | 监控面板 | 各 Agent 耗时、费用、冲突次数、工具调用、沙盒失败、审查驳回统计 |
| LF-05 | 故障隔离 | 单 Agent/单模型故障不影响整个房间主流程（可降级继续） |

---

## 13. 执行阶段补充（步骤零→四，按需启用）

> **定位**：本节是对「需要工具落地」任务的实施补充，**不是**房间主范式。  
> 房间主范式见 §1.1 Chat Review。  
> **任务广度**：利用大模型思考能力处理各类问题——如生成小游戏、做 Excel 表、读取并总结文档、写方案、以及（示例）爬取网站信息等。  
> 下列步骤一中的「反爬/Playwright」仅为环境自适应示例，编码/表格/文档类任务按自身环境维度检查即可。

### 步骤零：模型配置与平台纳管

| 编号 | 能力项 | 说明 |
|------|--------|------|
| P0-01 | 配置项完备 | 显示名、API Key、baseURL、模型 ID、超时、是否启用 |
| P0-02 | 添加模型入库 | 配置保存后进入平台模型目录，供角色绑定 |
| P0-03 | 分配前校验 | 未配置或探活失败的模型不可被房间选用 |
| P0-04 | 多模型就绪态 | 展示已就绪大脑列表，供本轮首答/审阅/评判认领绑定 |

### 步骤一：环境检查与动态调方案（按任务类型）

| 编号 | 能力项 | 说明 |
|------|--------|------|
| P1-01 | 任务环境画像 | 按任务识别检查项：依赖、权限、文件是否存在、站点可达性等 |
| P1-02 | 实地侦察（示例） | 网络类任务可用浏览器踩点；文档类则先读目标文件是否可访问 |
| P1-03 | 障碍识别（示例） | 如 Cloudflare 验证码等 → 结构化记录 |
| P1-04 | 否决不可行路线（示例） | 如放弃纯 request，改 Playwright/Selenium |
| P1-05 | 动态调整技术方案 | 把新约束写入共享记忆，供后续作答/实现引用 |
| P1-06 | 侦察报告进房间 | 结论对修订者与评判者可见 |
| P1-07 | 运行依赖检查 | 小游戏/脚本所需运行时、Excel 库、浏览器驱动等 |

### 步骤二：目标解构、讨论分工与骨架分段编写

| 编号 | 能力项 | 说明 |
|------|--------|------|
| P2-01 | 三件套解构 | 总体目标、任务清单、核心约束 |
| P2-02 | 角色间讨论分配 | 多模型在房间内讨论认领；仍保持会话隔离 |
| P2-03 | 评判/决策确认分解 | 清单确认后再开写 |
| P2-04 | 自顶向下骨架 | 先框架后细节（代码、表格结构、文档大纲同理） |
| P2-05 | 逐段填充 | 一次一段，避免一次生成超大体量 |
| P2-06 | 分段短检 | 关键段可经短修订/自检再继续 |
| P2-07 | 体量纪律 | 单次生成软上限；超限拆段 |

### 步骤三：静态检查与沙盒自测（可执行产物）

| 编号 | 能力项 | 说明 |
|------|--------|------|
| P3-01 | 禁止直接当最终交付 | 可执行/可验证产物须先后台纠错 |
| P3-02 | 静态检查 | 语法/结构/基础校验 |
| P3-03 | 沙盒执行 | 隔离运行；采集日志 |
| P3-04 | 成功放行 | 通过后进入结果审查或回到 chat review 评判 |
| P3-05 | 失败回灌 | 报错作为上下文交实现/修订角色修改 |
| P3-06 | 纠错提示模板 | 「刚刚写的代码报错 {类型}，根据报错重新修改…」 |
| P3-07 | 有限重试 | 超限升级用户或评判者 |
| P3-08 | 过程可选可见 | 默认后台；用户可展开日志 |

### 步骤四：业务结果审查与交付 / 重规划

| 编号 | 能力项 | 说明 |
|------|--------|------|
| P4-01 | 能跑 ≠ 正确 | 无报错只证明能运行 |
| P4-02 | 审查角色/Prompt | 可与 chat review 评判者合一或分立 |
| P4-03 | 对照模板 | 目标：…；当前结果：…；差距：…；通过/不通过 |
| P4-04 | 业务失败例 | 表为空、总结跑题、小游戏无法操作、爬取结果为空等 |
| P4-05 | 通过→交付/最终回复 | 可进入用户最终回复或文件交付 |
| P4-06 | 不通过→re-plan | 重规划并改产物，再测再审 |
| P4-07 | 修订闭环 | 改定位符/逻辑/表格字段/文档结构等后重跑 |
| P4-08 | 证据留存 | 目标、结果摘要、审查意见入记忆与审计 |

### 主范式与执行补充的关系

```
【主】Chat Review v2
  提问 → 自主认领 → 首答(区块稿) → 审阅(已读/沉默或实质PATCH)
  → 评判(点合入/冲突合并/R1·R2(A)·R3) → +1确认轮 → 最终回复
                      │
                      └─ 若需落地产物
                            ↓
【补】执行：步骤零→一→二→三→四 → 回到评判/确认轮门禁
```

---

## 能力域覆盖核对

| 能力域 | 覆盖章节 |
|--------|----------|
| 共享聊天室愿景与 Chat Review v2 | §0、§1、§1.1 |
| 多 Agent 编排 | §2 |
| 持久身份 | §3 |
| 跨模型互审 | §4 |
| A2A 通信 | §5 |
| 共享记忆 | §6 |
| Skills | §7 |
| MCP | §8 |
| 协作纪律 | §9 |
| 多模型决策大脑 | §10 |
| 工具与多样化交付 | §11 |
| 执行阶段补充（步骤零→四） | §13 |
| Windows 房间运维 | §12 |

---

## 成功判据（聊天室视角）

1. 平台首先是多模型共享聊天室：共享可见稿上完成审阅/补丁/评判，而非单模型独白。  
2. **已读+无补丁**才算同意；琐碎修改被拒收；实质分歧只改区块。  
3. 任意实质补丁须评判点合入；冲突由评判直接合并；合入后有 +1 确认轮。  
4. 打回分档可用：R1 局部 / R2 方向（旧稿作废仅 keep 参考）/ R3 微调；评判直读用户原话、不参与过程讨论。  
5. 首答模型 ≠ 评判模型；单模型时用户当评判。  
6. 各参与者会话隔离、不共用模型缓存；多样化任务可做；落地时才启用执行补充。  
7. 全过程可审计、可回放。  
