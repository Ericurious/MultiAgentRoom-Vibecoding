# 多模型协作 Agent 功能清单

> **产品愿景**：为多个模型创造共享「聊天室」——多 Agent 沟通平台。  
> **主范式（Chat Review v2）**：共享可见稿 + 自主认领职责 + 已读且无补丁才算同意 + 仅实质局部补丁 + 评判点合入/打回分档(R1·R2(A)·R3)/微调；首答模型 ≠ 评判模型；单模型时用户当评判。  
> **硬约束**：各参与者独立思考、会话隔离，不得共用同一模型缓存结果。  
> **实施补充**：需落地时再启用执行阶段（步骤零→四）；爬取仅为示例。  
> 运行环境：Windows；本文档：功能与实现要点清单（不含代码）。

---

## A. 平台总目标与边界

| 编号 | 功能项 | 实现要点（非代码） |
|------|--------|-------------------|
| A-01 | 多模型共享聊天室 | 核心是沟通容器：共享工作稿上多模型协作 |
| A-02 | Chat Review v2 主范式 | 首答→审阅(沉默/补丁)→评判合入或打回→+1确认轮→最终回复 |
| A-03 | 会话隔离 | 每参与者独立模型会话；禁止共享供应商侧缓存 |
| A-04 | 显式上下文下发 | 材料由平台打包；评判另直读用户原话 |
| A-05 | 多样化思考任务 | 小游戏、Excel、读文档总结、方案、爬取（示例）等 |
| A-06 | 执行阶段为补充 | 步骤零→四按需启用，不取代 chat review |
| A-07 | 沉默同意 | 仅「已读 + 无有效补丁」算同意；超时≠同意 |
| A-08 | 能跑≠正确 | 可验证产物另区分运行成功与结果正确 |
| A-09 | 最终回复门禁 | 未点合入/未关闭打回/确认轮未沉默通过 → 不得最终回复 |
| A-10 | 单模型降级 A | 仅一模型时用户任评判，禁止模型自评通过 |

---

## A2. Chat Review v2 功能项（主交互）

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| A2-01 | 用户提问与原话锚定 | 原始输入常驻；评判必须引用以防目标漂移 |
| A2-02 | 自主认领职责 | Agent 竞选首答/审阅/评判；开轮后冻结 |
| A2-03 | 首答区块化稿 | 产出带区块 ID 的共享基准稿 |
| A2-04 | 全员可见 | 稿、有效补丁、裁定对参与者可见 |
| A2-05 | 已读登记 | 审阅前须已读；发 PATCH 视为已读 |
| A2-06 | 沉默=同意 | 已读且本轮无有效补丁 |
| A2-07 | 实质 PATCH only | 一贴一块；含 claim + replace；琐碎系统拒收 |
| A2-08 | 琐碎禁言 | 形容词/语法/不重要步骤/重复步骤/&lt;100字非实质 → 拒收 |
| A2-09 | 评判点合入 | **含单条无冲突实质补丁**也须评判点击合入 |
| A2-10 | 冲突直接合并 | 同块多补丁：评判一次合并+说明，默认不开互辩 |
| A2-11 | 合并后 +1 确认轮 | 合入/微调/局部重交后强制多一轮；之后封顶升用户或再点合入不再自动+1 |
| A2-12 | 首答模型≠评判模型 | 硬约束；资格锁 |
| A2-13 | 评判不参与讨论 | 仅 VERDICT/MERGE/TWEAK/REJECT；上下文=原话+当前稿+待决补丁 |
| A2-14 | 通过→最终回复 | 确认轮沉默同意且无未决打回 |
| A2-15 | 与执行衔接 | 落地后仍回本门禁 |

---

## A3. 打回分档与评判权（R1 / R2(A) / R3）

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| A3-01 | R1 局部打回 | 大部可接受；只打回若干区块；未点名块锁定 |
| A3-02 | R2 方向打回 | 须说明书：锚定用户原话 + 错在方向 + 应回方向 + keep/discard |
| A3-03 | R2 策略 A | **旧稿全文作废**；keep 列表仅作参考；必须新首答 |
| A3-04 | R2 与大部接受互斥 | 不可同时 R2 又宣布终稿大面积接受 |
| A3-05 | R3 评判微调 | 不经他模，直接改函数/实现小疵；禁改方向/契约/验收；须 diff |
| A3-06 | R1+R3 可并存 | 接受大部分并可微调，同时少数块打回 |
| A3-07 | 不合格打回拒收 | 润色级意见不得点 R2 |
| A3-08 | 有打回无终稿 | reject 未关闭不得最终回复 |
| A3-09 | 同块打回上限 | 超限升级用户或换作答模型 |
| A3-10 | 微调进确认轮 | R3 后仍走 +1 轮供实质异议 |
| A3-11 | 合并说明可追溯 | 冲突合并须记录候选补丁与采纳理由 |
| A3-12 | critical 可升用户 | 高风险冲突/方向打回可配置强制用户确认 |

---

## B. 模型接入与运行时隔离

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| B-01 | 开源模型 API 接入 | 统一适配本地/远程开源推理服务的鉴权、限流、超时 |
| B-02 | 闭源模型 API 接入 | 统一适配主流商业模型 API；密钥分 Agent/分环境保管 |
| B-03 | 模型目录 | 维护模型能力标签（长上下文、代码、推理、多模态等）与单价/限速 |
| B-04 | Agent↔模型绑定 | 一 Agent 绑定主模型 + 备用模型列表 |
| B-05 | 独立会话句柄 | 每 Agent 独立 API 会话/请求链；禁止合并请求上下文 |
| B-06 | 显式上下文打包 | 审阅者：共享稿+区块；评判：+用户原话，默认不喂过程闲聊 |
| B-07 | 无共享 KV/缓存约定 | 不把 A 的 cache key 交给 B |
| B-08 | 输出标准化 | 已读、PATCH、VERDICT、R1/R2/R3、弃权等 |
| B-09 | 健康检查与熔断 | 连续失败、429、超时触发熔断与备用切换 |
| B-10 | 费用与配额账户 | 按房间、用户、Agent 统计 token 与费用 |
| B-11 | 模型配置录入（步骤零） | API Key、baseURL、模型名；保存即纳管 |
| B-12 | baseURL 必填策略 | 兼容官方与自建/代理端点 |
| B-13 | 添加后探活 | 失败禁止分配 |
| B-14 | 配置与房间解耦 | 先纳管再绑定本轮职责 |

---

## C. 用户与模型沟通

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| C-01 | 用户提问入口 | 问题/任务、附件、路径、验收标准进入聊天室；原话钉选 |
| C-02 | 职责指定或竞选 | 用户可指定，或开放 Agent 自主认领 |
| C-03 | 用户任评判 | 单模型降级 A，或用户主动接管评判 |
| C-04 | 澄清问答 | 可向用户提问；未答复可暂停 |
| C-05 | 批准/拒绝 | 高风险落盘、critical 冲突、过多 R2 |
| C-06 | 过程可见 | 可展开补丁流与裁定；默认可只看最终回复 |
| C-07 | 中断与改道 | 可停确认轮、换首答模型、追加原话约束 |

---

## D. 模型与模型沟通（A2A）

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| D-01 | A2A 消息总线 | 房间内广播共享稿事件与点对点必要通信 |
| D-02 | 消息类型协议 | 已读、PATCH、弃权、MERGE 结果、VERDICT、R1/R2/R3、工具请求 |
| D-03 | 审阅自闭环 | 审阅窗→评判→+1确认轮，直至最终回复或达上限 |
| D-04 | 与执行衔接 | 落地回灌后再 PATCH/评判 |
| D-05 | 沟通前工具钩子 | 只读检索等 |
| D-06 | 沟通后工具钩子 | 授权后写盘；回执进共享区 |
| D-07 | 区块引用 | 消息必带区块 ID（补丁类） |
| D-08 | 不可伪造身份 | 绑定角色/模型 ID |
| D-09 | 传递最小化 | 按区块下发，避免全量泄漏 |
| D-10 | 跨模型不共享缓存 | 只传平台消息对象 |

---

## E. 多 Agent 编排

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| E-01 | 能力标签 | 可声明擅长领域，供认领参考 |
| E-02 | 编排策略 | **默认 Chat Review v2**；执行子流程按需 |
| E-03 | 自主认领 + 资格锁 | 可切换职责；本轮已首答模型不可评判 |
| E-04 | 子任务图 | 复杂目标可多线并行 review |
| E-05 | 阶段门控 | 点合入 / 打回关闭 / 确认轮沉默 → 最终回复 |
| E-06 | 超时编排 | 未已读超时不视为同意；可催促或弃权 |
| E-07 | 人机混编 | 用户可任评判或强制介入 |
| E-08 | 预算编排 | 补丁/R2/确认轮耗尽则升用户 |
| E-09 | 单模型降级 A | 自动切换用户评判模式 |

---

## F. 持久身份

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| F-01 | Agent Profile | 稳定 ID、显示名、职责、语气、专业域、禁区 |
| F-02 | 权限契约 | 长期有效的工具/路径/MCP 授权集 |
| F-03 | 模型绑定档案 | 主备模型、温度等生成参数偏好（不含跨 Agent 共享缓存） |
| F-04 | 历史信誉 | 被采纳率、修订通过率、评判严苛度等 |
| F-05 | 跨房间延续 | 同一身份进入新房间时携带档案与必要长期记忆指针 |
| F-06 | 防冒充 | 发言、工具调用均需身份令牌；审计绑定身份 |

---

## G. 跨模型互审

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| G-01 | 独立审阅 | 各 Agent 独立会话读共享稿 |
| G-02 | 与 v2 对齐 | 实质 PATCH + 评判合入/打回即主互审 |
| G-03 | 评议维度 | 题意、正确性、完整性、可行性、风险、一致性；不含文采偏好 |
| G-04 | 事实核对 | 关键事实可工具取证后再 PATCH |
| G-05 | 局部修订闭环 | R1 只重开被打回块 |
| G-06 | 方向重置 | R2(A) 作废旧稿，新首答 |
| G-07 | 盲审选项 | 可选隐藏首答模型品牌 |

---

## H. 共享记忆（平台层）与私有记忆

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| H-01 | 共享记忆库 | 目标、约束、通过稿、待办、交付索引 |
| H-02 | 私有记忆库 | 各角色草稿与中间态，默认不对其他角色可见 |
| H-03 | 写入准入 | 共享写入宜在评判通过或明确规则下发生 |
| H-04 | 检索器 | 语义检索 + 过滤（权限、时间、任务、标签） |
| H-05 | 版本化事实 | 冲突记忆保留版本与裁定记录 |
| H-06 | 上下文编译器 | 为每次模型调用编译「最小充分」上下文 |
| H-07 | 与缓存隔离协同 | 记忆共享 ≠ 模型缓存共享；每次调用重新注入必要记忆 |
| H-08 | 擦除与归档 | 任务结束后归档；敏感字段支持擦除策略 |

---

## I. Skills 体系

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| I-01 | Skill 定义 | 名称、适用场景、输入输出契约、步骤、所需工具 |
| I-02 | Skill 商店/目录 | 房间或项目可启用的 Skill 集合 |
| I-03 | 绑定与授权 | 哪些身份可运行哪些 Skill |
| I-04 | 文档生成 Skill | 区块首答→实质补丁→评判合入/打回→确认轮→落盘 |
| I-05 | 概括 Skill | 读取并总结文档/多文件 → 要点与风险 |
| I-06 | 实现 Skill | 小游戏/脚本等：骨架→分段；禁止首轮千行倾倒 |
| I-07 | 表格/Excel Skill | 生成或填写结构化表 |
| I-08 | 环境侦察 Skill | 按任务需要侦察（网络踩点等为可选示例） |
| I-09 | 沙盒自测 Skill | 可执行产物入沙盒；失败回灌修订角色 |
| I-10 | 结果审查 Skill | 目标 vs 当前结果；可与评判角色合并 |
| I-11 | Skill 编排入口 | **主链 Chat Review v2**；执行 Skill 按需挂载 |
| I-12 | Skill 可观测 | 输入、输出、耗时、沙盒/审查记录进审计 |

---

## J. MCP 与工具层

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| J-01 | MCP Client 宿主 | 平台统一连接并管理多个 MCP Server |
| J-02 | 工具发现与 schema 校验 | 调用前按 schema 校验参数 |
| J-03 | 按身份授权 | 角色只能看见并调用被授权工具 |
| J-04 | Windows 本机工具 | 文件系统、终端、路径选择、通知等 |
| J-05 | 服务器侧工具 | SSH/远程代理/远端 MCP，用于服务器修改 |
| J-06 | 沟通前只读默认 | 作答/修订阶段默认只读；写工具需授权 |
| J-07 | 沟通后执行 | 评判通过或明确授权后执行，回执进房间 |
| J-08 | 变更回执 | 返回 diff、产物路径、退出码、日志摘要 |
| J-09 | 风险分级 | 读 / 项目内写 / 系统写 / 外网 / 删除 分级门控 |
| J-10 | 凭证隔离 | MCP 与 API 密钥按身份与环境隔离 |

---

## K. 协作纪律引擎

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| K-01 | 轮次上限 | 审阅窗、+1确认、R2次数、沙盒修订均设上限 |
| K-02 | 反附和 | 禁止「写得好」类发言；琐碎拒收 |
| K-03 | 打回须合格说明书 | R2 必须锚定用户原话与方向问题 |
| K-04 | 确认轮通过才最终回复 | 过程稿不得直接交付 |
| K-05 | 证据标签 | 已验证 / 推断 / 假设 |
| K-06 | 可追溯日志 | 首答→补丁→合入/R1·R2·R3→确认轮 |
| K-07 | 安全确认 | 高危二次确认 |
| K-08 | 防串台与防泄漏 | 私有草稿默认不进共享；评判不喂过程闲聊 |
| K-09 | 交付验收门 | 文本：确认轮；产物：另按需沙盒+结果审查 |
| K-10 | 审计回放 | 按房间回放 |
| K-11 | 禁止千行倾倒 | 编码超限拆段 |
| K-12 | 环境检查按需 | 可跳过无依赖侦察 |
| K-13 | 报错回灌纪律 | 沙盒失败须带异常类型与栈 |
| K-14 | 首答/评判模型隔离 | 同模型不得兼任；单模型→用户评判 |
| K-15 | 点合入纪律 | 无冲突单补丁亦须评判点击 |
| K-16 | 冲突少轮次 | 直接评判合并 |
| K-17 | R2 策略 A | 旧稿作废，keep 仅参考 |

---

## L. 最终决策与整合

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| L-01 | 评判者角色 | 非首答模型认领，或用户（降级 A） |
| L-02 | 输入隔离 | 直读用户原话 + 当前共享稿 + 待决补丁；不参与过程讨论 |
| L-03 | 点合入 / 冲突合并 | 单补丁也要点合入；冲突直接合并并说明 |
| L-04 | R1/R2(A)/R3 | 局部打回 / 方向打回作废旧稿 / 实现微调 |
| L-05 | 最终回复 | 确认轮已读+无补丁且无未决打回 |
| L-06 | 版本对比 | 区块级版本与补丁候选可对比 |

---

## M. 执行与交付（电脑 / 服务器）

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| M-01 | 本机项目作用域 | 默认限制在用户指定工作目录 |
| M-02 | 文档生成交付 | 报告/说明等写入约定目录 |
| M-03 | 内容概括交付 | 读文档总结、要点清单、对比表 |
| M-04 | 小游戏/代码交付 | 生成可运行小游戏或脚本；骨架→分段；附 diff |
| M-05 | Excel/表格交付 | 生成或填充表格文件 |
| M-06 | 多样化交付包 | 一次任务可多类型产物 + 交付清单（爬取仅为其中一类示例） |
| M-07 | 服务器变更 | 经授权远端执行；审计与回滚说明 |
| M-08 | 失败补偿 | 重试、回滚建议、人工接管 |
| M-09 | 完成通知 | 仅最终回复/交付门通过后通知 |
| M-10 | 按需环境侦察 | 网络/依赖类任务可浏览器踩点；文档类先验证可读 |
| M-11 | 技术方案切换器 | 按侦察结论切换实现栈（示例：request → Playwright） |
| M-12 | 沙盒运行时 | 隔离执行可验证产物 |
| M-13 | 报错上下文封装 | 异常类型、栈、相关片段回灌修订角色 |
| M-14 | 结果审查槽位 | 目标 vs 当前结果；可与 chat review 评判合并 |
| M-15 | re-plan 触发器 | 结果不通过则重规划并回跳 |

---

## M2. 执行阶段补充指导（步骤零→四，按需）

> **定位**：执行补充，服务于需要落地的任务；**主交互仍是 Chat Review v2（见 A2/A3）**。  
> **任务举例**（非穷尽）：生成小游戏、做 Excel 表、读取并总结文档、写方案；**爬取网站信息仅为环境自适应示例**。  
> 对应 `多Agent聊天室能力项.md` §13。

### 步骤零：在模型配置中添加 API 与 baseURL

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| M2-00-1 | 配置字段 | 显示名、API Key、baseURL、模型 ID、超时、启用开关 |
| M2-00-2 | 添加即纳管 | 写入平台模型目录，供作答/修订/评判等角色绑定 |
| M2-00-3 | 探活 | baseURL 试调用；失败不可用于房间 |
| M2-00-4 | 房间就绪 | 聊天室开工前确认可用模型已配置；单模型则启用用户评判 |

**指导要点：** 步骤零是平台级准备，支撑共享聊天室，不唯执行任务独有。

### 步骤一：环境检查与动态调整（按任务）

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| M2-01-1 | 按任务做环境画像 | 文档任务查路径可读；表格任务查库；游戏/脚本查运行时；网络任务查站点 |
| M2-01-2 | 侦察手段按需 | 需要时用浏览器等工具；不需要则跳过 |
| M2-01-3 | 障碍记录（示例） | 发现 Cloudflare 等 → 记入共享记忆 |
| M2-01-4 | 否决不可行路线（示例） | 放弃纯 request，避免后续仍按错误路线实现 |
| M2-01-5 | 切换方案（示例） | 改为 Selenium/Playwright，并更新约束 |
| M2-01-6 | 依赖检查 | 按选定方案检查本机依赖 |
| M2-01-7 | 报告进房间 | 供修订者与评判者引用 |

**指导要点：** 先弄清环境再动手；具体检查项随任务变化，不被爬取场景绑死。

### 步骤二：骨架生成与分段编写

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| M2-02-1 | 目标解构 | 总体目标、任务清单、核心约束 |
| M2-02-2 | 房间内讨论分配 | 多角色讨论认领；会话仍隔离 |
| M2-02-3 | 自顶向下 | 代码模块 / 游戏场景 / 表格结构 / 文档大纲先搭骨架 |
| M2-02-4 | 禁止一次超大体量 | 纪律或软拦截 |
| M2-02-5 | 逐段填充 | 一段一段实现 |
| M2-02-6 | 可穿插 chat review | 关键段可走短修订+评判再继续 |

**指导要点：** 用大模型思考能力做结构化分解，再实现；不要一次写千行。

### 步骤三：静态检查与沙盒自测

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| M2-03-1 | 可验证产物后台纠错 | 不直接当最终回复/交付 |
| M2-03-2 | 静态检查 | 语法/结构校验 |
| M2-03-3 | 沙盒执行 | 游戏可启动性、脚本退出码、表格可打开性等 |
| M2-03-4 | 成功才进结果审查 | 或回到 chat review ③ |
| M2-03-5 | 失败回灌 | 例：AttributeError → 回灌修订角色按报错修改 |
| M2-03-6 | 修订上限 | 超限升级用户 |

### 步骤四：业务结果审查与交付 / re-plan

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| M2-04-1 | 能跑≠正确 | 无报错不代表达标 |
| M2-04-2 | 审查可与评判合一 | 文本侧重确认轮；产物另加「目标 vs 结果」 |
| M2-04-3 | 对照模板 | 目标：…；当前结果：…；差距：…；通过/不通过 |
| M2-04-4 | 失败例 | 总结跑题、表为空、游戏无交互、爬取结果为空等 |
| M2-04-5 | 通过→最终回复/交付 | 进入用户最终回复或文件交付 |
| M2-04-6 | 不通过→re-plan | 重规划并修改产物 |
| M2-04-7 | 回跳 | 改实现回步骤二；方案错误回步骤一 |

---

## N. Windows 运行与工程化

| 编号 | 功能项 | 实现要点 |
|------|--------|----------|
| N-01 | Windows 服务/应用形态 | 桌面应用或后台服务 + **聊天室 UI** |
| N-02 | 路径与编码 | 盘符路径、PowerShell、UTF-8/系统编码 |
| N-03 | 本地配置 | 模型 API Key、baseURL、MCP、房间策略安全存储 |
| N-04 | 日志与诊断 | 含 chat review 轮次与（若有）沙盒/审查记录 |
| N-05 | 更新与插件 | Skill/MCP/模型适配器可增量更新 |
| N-06 | 资源占用治理 | 并发角色数与本机资源保护 |
| N-07 | 可选自动化依赖 | Playwright/Selenium 等按需，非平台唯一路径 |

---

## O. 端到端典型流程

### O1. 主流程：共享聊天室 + Chat Review v2（必经）

1. **步骤零（平台准备）**：配置 API Key + baseURL，探活，纳管模型  
2. 进入共享聊天室；Agent 自主认领本轮职责（资格锁：首答模型≠评判模型）  
3. 用户提问（原话钉选）；示例：小游戏 / Excel / 总结文档 / 其他  
4. 首答产出**区块化**共享稿  
5. 审阅窗：已读；无实质问题则沉默；有则只发实质 PATCH（琐碎拒收）  
6. 评判：点合入（含单补丁）/ 冲突直接合并 / R1·R2(A)·R3（直读原话，不参与讨论）  
7. **+1 确认轮** → 已读且无补丁则**最终回复**；否则再点合入（确认轮封顶）  
8. 单模型房间：步骤 6–7 的评判位为**用户**  

### O2. 执行补充（仅当需要落地产物时）

9. 按任务环境检查与方案调整（网络类可用踩点示例）  
10. 解构 → 骨架 → 分段实现  
11. 可验证产物：静态检查 → 沙盒；失败回灌  
12. 结果审查；不通过 re-plan；通过后交付并/或回到确认轮  
13. 归档与回放  

---

## P. 验收标准（平台级）

| 编号 | 验收项 |
|------|--------|
| P-01 | 多模型可在同一共享聊天室协作，输出彼此可见 |
| P-02 | Chat Review v2：已读+无补丁才同意；实质局部补丁；琐碎拒收 |
| P-03 | 任意实质补丁须评判点合入；冲突由评判直接合并 |
| P-04 | 合入后存在 +1 确认轮；通过后才最终回复 |
| P-05 | R1/R2(A)/R3 可用；R2 作废旧稿且须锚定用户原话 |
| P-06 | 评判不参与过程讨论并直读用户原话 |
| P-07 | 首答模型 ≠ 评判模型；单模型时用户评判（降级 A） |
| P-08 | 会话隔离，不共用模型缓存 |
| P-09 | 多样化任务示例可完成（小游戏/Excel/读文档总结等） |
| P-10 | 纯问答可不启用执行五段；需落地时执行补充可用 |
| P-11 | 模型配置支持 API + baseURL；Skills/MCP 可按需；可审计回放 |

---

## Q. 文档对照

| 能力域 | 本清单章节 |
|--------|------------|
| 愿景 / Chat Review v2 / 打回分档 | A、A2、A3、O1 |
| 多 Agent 编排 | E |
| 持久身份 | F |
| 跨模型互审 | G |
| A2A 通信 | D |
| 共享记忆 | H |
| Skills | I |
| MCP | J |
| 协作纪律 | K |
| 评判与合入 | L |
| 模型接入与隔离 | B |
| 执行补充与多样化交付 | M、M2、O2 |
| Windows 工程化 | N |

配套文档：`多Agent聊天室能力项.md`（§1.1 Chat Review v2 + §13 执行补充）  
本文件：功能与实现要点（A2/A3 主范式 + M2 执行补充）  
